-i  inventory 配置路径

-m command		#-m	后添加模块
-a	后加命令


 ansible  -i /opt/ansible/hosts/node  node -m ping


 ansible-playbook push-ssh.ymal 



 #检查yaml文件的语法是否正确
 ansible-playbook --syntax-check   first.yml 

  #检查tasks任务
 ansible-playbook  --list-task       first.yml

  #检查生效的主机
# ansible-playbook  --list-hosts   first.yml

#指定从某个task开始运行
ansible-playbook  --start-at-task='Copy Nginx.conf'      first.yml


# ansible-playbook first.yml -k      #用来交互输入ssh密码
# ansible-playbook first.yml -K      #用来交互输入sudo密码
# ansible-playbook first.yml -u      #指定用户


echo -n '123456' | ansible-vault encrypt_string --vault-id dev@a_password_file --stdin-name 'db_password'

--vault-password-file /opt/ansible/a_password_file 